# shellcheck shell=bash
# shellcheck disable=SC2034 # read by bin/dotctl
# Base setup for any Omarchy machine. Features needing Omarchy are skipped elsewhere.
FEATURES=(
  hyprland
  omarchy-shell
  kitty
  herdr
  mise
  apps
)
