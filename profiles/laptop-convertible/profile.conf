# shellcheck shell=bash
# shellcheck disable=SC2034 # read by bin/dotctl
# 2-in-1 laptop with an external monitor on HDMI. Adds tablet mode and dictation;
# home/ overrides hypr/monitors.lua with this machine's display layout.
PROFILE_EXTENDS=default
FEATURES=(
  tablet-mode
  voxtype
)
